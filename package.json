{
  "name": "recruitment-tracker-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix frontend install",
    "build": "npm --prefix frontend install && npm --prefix frontend run build",
    "start": "npx --prefix frontend vite preview --host 0.0.0.0 --port ${PORT:-10000}"
  }
}
